# DSH From Scratch 课程约定

本仓库是一个**教学项目**，不是产品代码：从零造一个 agent harness，一个痛点一个痛点地演进成 dsh。导览见 [README.md](README.md)，大纲见 [COURSE.md](COURSE.md)。作者可以是人，也可以是 AI 助手；两者都遵守本文件。

## 与参考源码的关系

`dsh/` 是钉在固定 commit 上的 git submodule，指向 [deepseek-harness](https://github.com/deepseek-ai/deepseek-harness)。它的作用是让讲义里的每一处"对照真实源码"都能被打开、被 grep、被跑起来，而且**不会因为上游改动而变成谎言**。

**终点是骨架 1:1。** 每个阶段的简化版都是过渡形态，不是终态：讲义在给出简化版之后，必须说清它与 `dsh/` 对应实现的差距，以及哪个阶段补齐。骨架范围见 [README.md](README.md#终点复刻到哪一步)；外围包只带读、不重写，讲义讲它的分层与取舍即可。

四条硬边界：

1. **`dsh/` 只读。** 永远不要在里面改任何东西，也不要提交 submodule 内的改动。发现 dsh 本身的 bug，去 dsh 仓库单独提。
2. **升级参考版本是一次独立的、显式的改动。** 步骤：进 `dsh/` 拉到新 commit → 回到课程根 `git add dsh` → 跑 `npm run check` → 修好所有失效引用 → 在 README 的"参考版本"里更新 commit。绝不能顺手带在别的提交里，因为它会同时改变全课程的对照面。
3. **课程代码不依赖 dsh 的构建产物。** 只用 Node 内置能力和本仓库自己的 `tsx`；不从 `dsh/` 里 import 任何东西。从零学的意思就是不借现成的包——第二阶段引入 Cordis 概念时，也是先自己写一个几十行的迷你版，再和 `dsh/vendor/cordis/` 对照。
4. **真实密钥永远不进仓库。** 配置文件里只出现环境变量名，`dsh-learn.json` 已在 `.gitignore` 里。

## 引用必须可核对

讲义里指向参考源码的路径一律写成 `` `dsh/packages/...` `` 这种反引号形式，`npm run check` 会逐条验证它们真实存在，同时验证所有 Markdown 相对链接可解析。

**提交前跑一次 `npm run check`。** 这是这门课唯一的自动化防线：讲义会腐烂，而它是唯一会告诉你的东西。写不准的路径不要凭印象填——先打开文件。

## 教学法（这门课怎么教）

### 动机驱动

- **先痛后药**：引入任何抽象之前，先用一节课把它解决的那个疼**复现出来**——具体到"改这个功能要动哪 3 个文件"、"重启之后哪条信息丢了"。没有痛点的抽象，读者只会背下来，不会理解。
- **痛点之后必须紧跟"解法全貌"，再拆细节**。顺序固定为：① 一句话说清方案是什么；② 改前 / 改后的数据流对照；③ 能一眼看完的完整代码（几十行，不是片段）；④ 用起来是几行；⑤ 产出长什么样。**做完这五步才可以逐点展开设计取舍。** 直接从痛点跳进"字段一、字段二"，读者手里没有全貌，每一点都不知道挂在哪儿。
- **奥卡姆剃刀**：能用简单方案解决的，先用简单方案。简化是过渡手段，每个简化版最后都要对照真实实现说清"dsh 为什么更复杂"。
- **用到再讲**：一个概念在它第一次被需要、能回答一个具体问题时才引入。当前代码不得不出现后续概念时，只用一句话标注"阶段 N 会讲"，不展开。
- **不留前向依赖**：写第 N 课时只能假设读者懂前 N-1 课讲过的东西。

### 代码即教材

- **不布置作业**：不要出现"动手做：请你实现一个 XX"。读者的时间花在**读懂**上，不是被考。
- **文档里的每段代码都要有对应文件**：讲义中出现的代码必须能在 `src/` 里打开、能跑、能下断点。不要只在文档里贴片段。
- **详细注释**：教学代码的注释要说明"这段在做什么"和"为什么这样写"，因为读者可能第一次见这个语法。改代码必须同步改注释，不允许注释和代码对不上。
- **教 debug**：每节课至少给一处"跑不通时怎么定位"的具体手法——读哪一行报错、打什么日志、在哪下断点。只给正确答案的课是失败的课。

### 对照真实源码

每个阶段的实现之后，都要带读者看 dsh 的对应实现，并回答三个问题：

1. dsh 是怎么做的（给出 `dsh/packages/…` 的**真实路径**，写之前必须打开确认过）
2. 它为什么更复杂（多出来的复杂度分别买到了什么：持久化？取消？权限？跨进程？）
3. 我们的简化版会在**哪一个阶段**被补齐

对照表的形式最好用：`| | 我们的 | dsh 的 | 为什么 dsh 更复杂 |`。

### 阶段验收

每个阶段最后一节固定是"阶段验收"，包含：可执行的验收清单（命令 + 预期输出）、本阶段新增/修改了哪些文件、与 dsh 的差距表、**工程思维总结**（学到的是设计判断，不是 API 列表）、下一阶段的痛点预告。

## 文件组织

```
DshFromScratch/
├── README.md、COURSE.md、AGENTS.md
├── docs/<两位阶段号>-<阶段名>/<两位小课号>-<小课名>/<两位序号>-<主题>.md
├── src/                     # 教学代码，随阶段演进（不按阶段分目录）
├── scripts/check-course.mjs # npm run check
└── dsh/                     # 参考源码 submodule，只读
```

- 一个 `.md` 只讲一件事。判据是**话题是否单一**，行数只是提示：出现明显话题切换就拆成同目录下的多个带序号文件（`01-` 是主线，`02-`、`03-` 是从它拆出去的专题，彼此互链）。单一话题写到两百多行是正常的，硬拆只会把一条论证切断。
- 代码在 `src/` 里**原地演进**（阶段 3 修改阶段 1 写下的 `llm.ts`），因为真实开发就是这样；哪些文件被改动由该阶段的"阶段验收"说明。
- 讲义正文一段一行（不硬换行），和仓库其余文档保持一致。

## 课程大纲是活文档

[COURSE.md](COURSE.md) 只把**当前阶段**细化到小课级别，后续阶段保持"目标 + 痛点 + 引入的抽象 + 对照位置"四行。进入新阶段的第一件事是补细化，不是直接写讲义。每个阶段完成后勾上"当前状态"里的复选框，并改写文件末尾的"下一步"。

## 给 AI 助手的指令

1. **先问后做**：遇到设计选择（先讲哪个、简化到什么程度、用哪个类比），给出选项和各自代价，让学习者选。培养工程判断力本身就是课程目标之一。
2. **教工程思维，不教基础概念**：重点是设计决策、trade-off、为什么不选另一条路，而不是"什么是变量"。但读者对 TypeScript / Node 生态可以是零基础，语法要随用随讲。
3. **关联学习者已有的知识**：解释新概念时优先用他已经熟悉的语言/领域做类比，并**明确指出类比在哪里失效**——错误的类比比没有类比更贵。
4. **每段代码都要能回答"为什么这样写"**：如果一段代码的理由只是"教程里这么写"，那就把真正的理由找出来，或者换个写法。
5. **引用源码前先打开它**：路径、符号名、行为描述必须与仓库当前状态一致。写不准就去读，不要凭印象。
6. **中文为主，术语保留英文**：Cordis、waterfall、capability seam、session log、tool call 这类词保留英文，不要生造译名。
7. **一节课一个提交**：讲义、代码、演示脚本在同一个提交里，提交信息写清这一课教了什么。跑完 `npm run check` 和 `npm run typecheck` 再提交。
