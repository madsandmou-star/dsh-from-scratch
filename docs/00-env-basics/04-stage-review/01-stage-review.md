# 0.4 阶段 0 验收

> 本课目标：确认环境可用，并把这一阶段学到的**判断**沉淀下来。

## 验收清单

```sh
# 1. 能直接跑 .ts
npm run hello        # 等价于 node --import tsx src/hello.ts
# 期望输出：hello, dsh

# 2. 命令行参数能进来
node --import tsx src/hello.ts 小明
# 期望输出：hello, 小明

# 3. 调试器能停住
node --inspect-brk --import tsx src/hello.ts
# 期望：终端打印 Debugger listening on ws://127.0.0.1:9229/...，进程挂起等待接入
```

| 验收项 | |
|---|---|
| Node / 包管理器 / tsx 三个角色各自负责什么 | ✓ |
| `.ts` 直接跑起来，并知道类型检查是另一件事 | ✓ |
| 会读报错栈，能找到属于自己的那一帧 | ✓ |
| 会用 `console.dir(x, { depth: null })` 打完整结构 | ✓ |
| 会用 `--inspect-brk` 接调试器下断点 | ✓ |
| 知道 ESM 的两条规则：相对导入带 `.ts`、没有 `__dirname` | ✓ |

## 本阶段产出

```
src/hello.ts    # 唯一的代码
```

## 工程思维总结

### 1. 类型是给"修改"用的，不是给"输入"用的

TypeScript 在运行时什么都不做。它值钱的地方是：三个月后你改一个字段名，编译器把所有受影响的地方指给你看。它不值钱的地方是：外面进来的数据长什么样，它一无所知。

**所以边界在哪，校验就在哪。** dsh 把这条写成了明确的规矩——同进程内由类型保证的调用不做运行时校验，而配置、模型返回的 JSON、落盘文件、跨进程消息必须校验。下一阶段第一节就会用上它。

### 2. 一致性比个人偏好值钱

这门课用 `node --import tsx` 而不是别的跑法，理由不是"它更好"，而是**dsh 已经这么做了**。参考源码怎么跑，课程就怎么跑，你才能顺手把 `dsh/` 里的东西也跑起来对照。

反过来，课程用 npm 而 dsh 用 pnpm，也是同一条判断得出的结论：课程只有一个依赖（tsx），不需要 workspace，用 pnpm 只是徒增前置要求。**一致性是为了降低摩擦，不是为了整齐。**

### 3. 会 debug 才算会读代码

阶段 0 唯一真正重要的技能是断点和打点。后面每加一层抽象（工具循环、插件树、事件管线），"这行代码到底有没有被执行、执行时值是什么"就会难猜一分；到了阶段 10 的 waterfall 事件，光靠读代码几乎不可能推出实际的执行顺序——那时候你只有一个办法：停下来看。

## 阶段 0 学了什么

| 课 | 知识点 |
|---|---|
| 0.1 | Node / 包管理器 / tsx 的分工，monorepo 与 workspace，为什么课程和 dsh 是两个仓库 |
| 0.2 | 类型只在编译期、六个常用语法、`import type`、ESM 的两条规则、`strict` 严在哪 |
| 0.3 | 读报错栈、三种打点法、`--inspect-brk` 断点、三类故障的固定动作、dsh 的 runtime invariant |

## 下一阶段的痛点预告

现在你有一个能打印 hello 的程序。下一阶段要让它**说话**——调用真正的模型。

第一个要面对的问题不是代码，是**密钥往哪放**：写进代码会被提交上去，写进配置文件也会被提交上去。这个看起来很小的问题，在 dsh 里长成了一整条能力接缝（`dsh/packages/credentials/`）。

---

下一阶段：[阶段 1：最小 agent](../../01-minimal-agent/01-config-and-key/01-config-and-key.md)
